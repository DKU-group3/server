spring:
  datasource:
    url: jdbc:mysql://localhost:3307/taggo_test
    username: admin
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: update

jwt:
  secret: ${JWT_SECRET:your-very-secret-key}
  access-token-expiration: 3600000 # 1시간
  refresh-token-expiration: 1209600000 # 2주