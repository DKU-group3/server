spring:
  datasource:
    url: jdbc:mysql://mysql:3306/taggo?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: taggopw
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000

kakao:
  app-key: ${KAKAO_APP_KEY}
